# # Click Me - React Demo Bootstrapped with Vite

Start a development server with the following command.

```bash
npm run dev
```

Create a build with the following command.

```bash
npm run build
```

Preview the build with the following command.

```bash
npm run preview
```
